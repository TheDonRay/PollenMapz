import mongoose  from "mongoose"; 

// pollen schema but i dont know if i will need this since data is just being mapped on ? i still need to add the pollen data to the data folder in the project. 
const PollenDataSchema = new mongoose.Schema({ 

}); 

